name: hikaribot

services:
  hikaribot:
    image: ${HIKARI_PYTHON_IMAGE:-python:3.12-slim-bookworm}
    container_name: hikaribot-docker
    restart: unless-stopped
    depends_on:
      - napcat
      - cobalt
      - searxng
    environment:
      TZ: Asia/Shanghai
      PYTHONDONTWRITEBYTECODE: "1"
      PYTHONUNBUFFERED: "1"
      UV_PROJECT_ENVIRONMENT: /opt/hikaribot-venv
      UV_LINK_MODE: copy
    ports:
      - "${HIKARI_BOT_ADMIN_PORT:-${HIKARI_STICKER_WEB_PORT:-54213}}:54213"
      - "${HIKARI_MEDIA_DETAIL_WEB_PORT:-53123}:53123"
    working_dir: /app
    entrypoint: ["sh", "/app/docker/entrypoint.sh"]
    command: ["python", "bot.py"]
    volumes:
      - ./app:/app
      - ./app:/opt/hikaribot-source
      - ./BotData:/app/BotData
      - ./UserData:/app/UserData
      - ./runtime/shared:/app/sharedFolder
      - ./runtime/tmp/hikari_bot:/tmp/hikari_bot
      - hikaribot_venv:/opt/hikaribot-venv
    extra_hosts:
      - "host.docker.internal:host-gateway"
    # 显式指定国内 DNS：宿主机默认 DNS 对 b23.tv 等域名会返回被污染的记录，
    # 导致媒体解析连不上 B 站 CDN。
    dns:
      - 223.5.5.5
      - 119.29.29.29

  napcat:
    image: mlikiowa/napcat-docker:latest
    container_name: napcat
    restart: unless-stopped
    environment:
      NAPCAT_UID: "${NAPCAT_UID:-0}"
      NAPCAT_GID: "${NAPCAT_GID:-0}"
      ACCOUNT: "${NAPCAT_ACCOUNT:-}"
      TZ: Asia/Shanghai
    ports:
      - "${NAPCAT_WEBUI_PORT:-3000}:3000"
      - "${NAPCAT_WS_PORT:-3001}:3001"
      - "${NAPCAT_ONEBOT_PORT:-6099}:6099"
      - "${NAPCAT_REVERSE_WS_PORT:-54258}:54258"
      - "${NAPCAT_EXTRA_PORT:-3004}:3004"
      - "${NAPCAT_BOT_WS_PORT:-54253}:54253"
    volumes:
      - ./napcat/config:/app/napcat/config
      - ./napcat/ntqq:/app/.config/QQ
      - ./runtime/shared:/app/sharedFolder
      - ./legacy/pixiv_cache:/opt/HIKARI_BOT/data/pixiv_cache
      - ./runtime/tmp/hikari_bot:/tmp/hikari_bot

  cobalt:
    image: ghcr.io/imputnet/cobalt:11
    container_name: cobalt
    init: true
    read_only: true
    restart: unless-stopped
    ports:
      - "${COBALT_PORT:-54257}:9000/tcp"
    environment:
      API_URL: "${COBALT_API_URL:-http://127.0.0.1:54257/}"

  searxng:
    image: docker.io/searxng/searxng:${SEARXNG_VERSION:-latest}
    container_name: searxng-core
    restart: always
    ports:
      - "${SEARXNG_HOST:+${SEARXNG_HOST}:}${SEARXNG_PORT:-54261}:8080"
    volumes:
      - ./searxng/core-config:/etc/searxng:Z
      - searxng_core_data:/var/cache/searxng
    depends_on:
      - searxng-valkey

  searxng-valkey:
    image: docker.io/valkey/valkey:9-alpine
    container_name: searxng-valkey
    command: valkey-server --save 30 1 --loglevel warning
    restart: always
    volumes:
      - searxng_valkey_data:/data

  # ── HIKARI_AI: QQ 官方机器人（由 DeepSeek V4 驱动 + NEO 协同媒体解析） ──
  hikari-ai:
    image: node:22-slim
    container_name: hikari-ai
    restart: unless-stopped
    depends_on:
      - hikaribot
    environment:
      TZ: Asia/Shanghai
      NODE_ENV: production
    working_dir: /app
    volumes:
      - ./app/Officials/HIKARI_AI:/app
      - ./runtime/tmp/hikari_bot:/tmp/hikari_bot
      - hikari_ai_node_modules:/app/node_modules
    command: >
      sh -c "
        npm install -g pnpm@11 &&
        pnpm install --frozen-lockfile &&
        pnpm build &&
        exec node dist/index.js
      "

volumes:
  hikaribot_venv:
  hikari_ai_node_modules:
  searxng_core_data:
  searxng_valkey_data:
