# Changelog

## [1.2.1] - 2026-08-31

### 字体资源交付

- 将 Noto Sans CJK 字体二进制移至独立字体仓库，缩小插件发布包体积。
- 插件加载和文本图片渲染前会幂等检查字体；缺失或大小、SHA256 校验失败时，从固定版本的字体 Release 自动下载。
- 下载内容先写入唯一临时文件，完整校验后再原子替换；失败或取消时清理临时文件，不影响插件主流程和原文本回退。
- 同步更新字体配置提示、使用说明和架构文档。

## [1.2.0] - 2026-08-29

### 雪球帖子解析

- 新增雪球帖子解析器，支持 `xueqiu.com/{user_id}/{status_id}` 形式的分享链接。
- 先向 `xueqiu.com/service/csrf` 申请访客令牌，再经 `api.xueqiu.com/statuses/show.json` 读取帖子详情，绕开 HTML 页面的 JS 挑战与 WAF 拦截。
- 覆盖普通帖、长文、多图长文、转发帖和表情帖；正文按阅读顺序内嵌配图，`pic` 与 `image_info_list` 用于补齐并按原图地址去重，表情图片还原为文字标记。
- 访客令牌过期（业务码 `400016`）时自动重新申请并重试一次。
- 命中 HLS 播放地址时补 `m3u8:` 前缀并标记为强制缓存后发送。
- 新增 `parsers.xueqiu` 输出模式配置项。

### 解析器结构维护

- 抖音的解析器入口与专属辅助模块收拢为 `core/parser/platform/douyin/` 子包（`parser.py` / `sign.py` / `web.py`），其余 12 个平台维持一平台一文件。
- 解散抖音与 TikTok 共用的跨平台 mixin，辅助方法按平台各自内联；`core/parser/` 不再保留跨平台共享 mixin。
- 本地调试入口的平台模块遍历改为递归进入平台子包，解析器发现顺序补齐至 13 个平台。
- 统一 `core/parser/` 项目内导入组的组内顺序为按相对层级由深到浅。
- 将解析器模块落位规则写入 `AGENTS.md`，`core/parser/` 目录结构说明写入 `docs/ARCHITECTURE.md`。
- 更正 `AGENTS.md` 中 `parse()` 失败语义的表述：解析失败时抛出异常，由 `ParserManager` 统一转换为含 `"error"` 键的结果。
- 上述调整均为内部结构整理，不改变解析、下载和消息输出的对外行为。

## [1.1.0] - 2026-08-24

### Steam 游戏页解析

- 新增 Steam 游戏页解析器，支持 `/app/{appid}` 及带 slug 的游戏页链接。
- 调用 Steam `appdetails` 接口提取标题、简介、发行信息、价格、截图和预告片。
- 新增 `steam.use_xiaoheihe` 配置，可改用小黑盒完整接口获取评分、在线人数、峰值、销量排行和平均游戏时间等统计。
- 新增 `proxy.steam.parse`、`proxy.steam.video` 和 `proxy.steam.image` 三项代理开关，分别控制详情解析、视频下载和图片下载。
- 更新解析器配置面板、平台文档、架构说明和本地调试解析器发现顺序。

### 文本元数据图片渲染

- 新增将文本元数据、热评和翻译内容合并渲染为单张图片的可选功能。
- 支持清新便签、科技感、专业严肃和温和卡片样式，以及字体族和字号配置。
- 新增 Pillow 依赖；支持通过 `ASTRBOT_MEDIA_PARSER_FONT` 指定中文字体文件。
- 图片支持文件令牌中转；渲染或发送失败时自动回退为原文本节点。

## [1.0.0] - 2026-08-23

本版本将主线下载、解析、缓存和消息输出链路整理为稳定基线，重点处理多平台回退和异常边界。

### 下载与媒体处理

- 重构普通下载、Range 下载、M3U8、DASH、封面截帧和图片格式转换的处理路径。
- 统一媒体大小限制、响应校验、临时文件清理和缓存回收行为；下载失败时按媒体项独立回退，避免单项错误中断整条消息。
- 保留 B 站 DASH 高画质下载和 MP4 回退，并强化视频封面发送场景下的失败处理。

### 平台解析

- B 站使用 WBI Web 接口，支持 Cookie 运行时校验、扫码登录、凭证持久化和管理员协助更新。
- 抖音使用 Web 解析与签名流程，统一视频、图集和分享链接的边界处理。
- 优化小红书、快手、TikTok、闲鱼、今日头条和 Pixiv 的回退路径与异常提示。

### 消息与归档

- 解耦消息聚合、翻译、媒体发送和引用链接 ZIP 归档流程。
- 完善归档目录、媒体清理、消息节点构建和中转文件的生命周期管理。

### 修复与维护

- 修复网络重定向、代理、临时文件、缓存清理和平台返回异常导致的级联失败。
- 小黑盒签名将规则中的 8 字节单密钥明确规范化为 `key + key + key` 的 24 字节 TripleDES key，保持签名结果不变并消除新版 cryptography 的弃用警告。
- 对不支持的 DES key 长度增加明确校验，避免静默生成错误签名。
- 同步更新配置 schema、README、架构说明和解析方法文档。

## [0.9.0] - 2026-08-01

### Pixiv 解析

- 支持 `artworks`、`i` 以及带 `/en/` 前缀的插画和漫画链接。
- 提取作品标题、作者、标签、页数、R-18/R-18G 和 AI 生成标记，并支持多页图片。
- 新增 Pixiv Cookie 和代理配置，代理同时覆盖 Web API 请求和图片下载，支持登录及年龄限制作品。
- 图片按原图到较低分辨率自动降级，并补充 Cloudflare 拦截提示和解析并发限制。

### ZIP 归档

- 新增引用链接 ZIP 归档命令，归档包含 `metadata.txt`、媒体文件和稳定的目录层级。
- ZIP 文件通过 AstrBot 文件消息发送，发送完成或失败后自动清理临时文件。

### 平台兼容

- 新增小红书 `xhslink.cn` 短链域名解析支持。
- Pixiv Cookie 配置调整到平台增强设置区域，保留原有配置键。

## [0.8.1] - 2026-07-12

### 文本与消息输出

- 文本元数据支持分别控制标题、作者、发布时间、原始链接和简介/正文的显示。
- 翻译、热评和文本元数据节点拥有明确的输出边界；隐藏字段不会提交给翻译模型。
- 重构消息输出配置域，明确文本元数据、富媒体、热评、开场语和消息聚合的职责。
- 将文本元数据、热评和翻译节点按 4000 字限制拆分，降低超长消息发送失败概率。

### 修复

- B 站 Cookie 协助流程忽略 Notice、Request 等非用户消息事件，避免误消费确认状态。
- 清理翻译和消息发送流程中的冗余配置路径与旧测试实现。

## [0.8.0] - 2026-06-21

### 抖音与解析控制

- 抖音 `slides` 多分段作品支持提取分段视频和封面，不再统一当作图片处理。
- 新增“视频仅发送封面”模式；优先使用解析结果中的封面，无封面时尝试截取视频首帧。
- 新增同链接和同用户解析频率限制，记录按时间窗持久化并自动裁剪。

### 优化

- 补充抖音、快手和 Twitter/X 的视频封面元数据，提高封面模式命中率。
- 恢复并改进本地调试脚本，支持自动发现平台解析器。

## [0.7.1] - 2026-04-28

### 新增平台

- 新增闲鱼商品页解析，支持商品短链和商品详情页。
- 新增今日头条文章、视频和微头条解析，支持短链、文章页、视频页与微头条页。
- 补充对应平台的文本、图片和视频媒体字段，接入统一消息输出流程。

## [0.7.0] - 2026-04-27

### 新增平台

- 新增 TikTok 视频和图集解析，支持短链、长链、视频作品、图片作品及按配置使用代理。
- 新增小黑盒帖子媒体解析，可从分享链接提取正文、图片和视频；相关能力需要 `cryptography` 依赖。

### 下载与缓存

- 重构媒体下载决策和缓存清理，按单个媒体判断本地缓存、直链发送或跳过，并在文本中说明原因。
- 使用缓存标记目录替代旧缓存注册表，降低误删风险。
- Twitter/X 优先使用现有解析服务，服务不可用时自动尝试备用路径；快手补充图集扩展字段识别。

## [0.6.0] - 2026-03-25

### 新增

- 新增 AstrBot HTTP 媒体中转，可将本地缓存文件转换为协议端可访问的临时 URL。
- 新增部分平台热评获取能力，并将热评作为独立消息节点输出。

### 优化与修复

- 重构配置文件分组和 README/架构文档，明确平台代理、消息输出和媒体中转配置边界。
- 修复部分边界情况下的错误处理，改善媒体文件发送和清理流程。

## [0.5.0] - 2026-03-14

### 下载与 B 站增强

- 新增 DASH 下载处理器，支持视频/音频分流下载、合并和独立的 Range 加速。
- 新增 B 站 Cookie 运行时认证，支持 Cookie 校验、二维码扫码登录和凭证持久化。
- 新增管理员协助登录流程，Cookie 失效时可私聊通知管理员重新登录。

### 下载管线

- Range 下载失败时自动回退普通下载，下载管线增加 URL 前缀路由、关停机制、403 检测和按项代理。
- 配置系统改为分组结构，补充平台代理和 Cookie 相关配置。
- 消息适配器支持访问状态展示、大文件单独发送和纯图片画廊。

## [0.4.1] - 2026-03-02

这是贡献者维护的 YouTube 解析预览版，属于 prerelease，代码来自独立功能分支，不代表主线稳定能力。

- 增加 YouTube 视频链接解析能力。
- 接入现有媒体下载和消息发送流程，提供视频元数据和媒体直链。

## [0.4.0] - 2025-12-18

### 新增与重构

- 新增小黑盒解析器和 Debug 模式。
- 整理解析器、下载器、消息适配器和缓存模块边界，建立后续平台扩展所需的架构基线。

### 修复

- 修复解析成功前提前发送提示文本的问题。
- 适配小红书平台最新风控策略，收窄链接匹配逻辑以跳过直播专用 `live` 子域名。

## [0.3.0] - 2025-11-17

### 新增

- 新增微博解析，支持微博正文、图片、视频和基础文本元数据。

### 优化

- 精简解析器返回值，统一直链有效性验证逻辑。
- 非预下载模式下，图片默认保存到临时缓存后发送，提高受限 CDN 场景下的发送成功率。

## [0.2.0] - 2025-11-13

### 新增

- 稳定小红书解析能力。
- B 站解析器支持 QQ 小程序卡片链接。

### 修复与重构

- 修复小红书解析失败问题。
- 完成解析流程和项目结构重构，整理为经过测试的稳定版本。

## [0.1.0] - 2025-11-10

### 初始稳定版

- 确立插件首个稳定发布基线。
- 完成基础链接提取、平台解析、媒体下载、消息节点构建和临时文件清理流程。
- 同步首版配置项、使用文档和插件市场元数据。
